
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="lista_zakupow.MainPage"
             BackgroundColor="#66717E">

    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.1*"/>
            <RowDefinition Height="0.8*"/>
            <RowDefinition Height="0.1*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <VerticalStackLayout
    Grid.Row="0"
    Grid.Column="3"
    Grid.ColumnSpan="1"
    Grid.RowSpan="3"
    Margin="0,110,0,0">

            <Button
        Text="Usuń"
        x:Name="Delete"
        Clicked="Delete_Clicked"
        WidthRequest="180"
        HeightRequest="40"
        BackgroundColor="#383B53"
        TextColor="#D4D6B9"
        FontSize="16"
        CornerRadius="10"
        BorderWidth="0"
        Padding="0"
        Margin="30">

                <Button.Shadow>
                    <Shadow
                Brush="#000000"
                Offset="4,5"
                Radius="10"
                Opacity="0.30" />
                </Button.Shadow>

            </Button>

            <Button
        Text="Zeskanuj nowy kod"
        x:Name="AddQR"
        Clicked="AddQR_Clicked"
        WidthRequest="180"
        HeightRequest="40"
        BackgroundColor="#383B53"
        TextColor="#D4D6B9"
        FontSize="16"
        CornerRadius="10"
        BorderWidth="0"
        Padding="0"
        Margin="30">

                <Button.Shadow>
                    <Shadow
                Brush="#000000"
                Offset="4,5"
                Radius="10"
                Opacity="0.30" />
                </Button.Shadow>

            </Button>

            <Button
        Text="Edytuj"
        x:Name="EditButton"
        Clicked="EditButton_Clicked"
        WidthRequest="180"
        HeightRequest="40"
        BackgroundColor="#383B53"
        TextColor="#D4D6B9"
        FontSize="16"
        CornerRadius="10"
        BorderWidth="0"
        Padding="0"
        Margin="30">

                <Button.Shadow>
                    <Shadow
                Brush="#000000"
                Offset="4,5"
                Radius="10"
                Opacity="0.30" />
                </Button.Shadow>

            </Button>

            <Button
        Text="Edycja bazy danych"
        WidthRequest="180"
        HeightRequest="40"
        BackgroundColor="#383B53"
        TextColor="#D4D6B9"
        FontSize="16"
        CornerRadius="10"
        BorderWidth="0"
        Padding="0"
        Margin="30">

                <Button.Shadow>
                    <Shadow
                Brush="#000000"
                Offset="4,5"
                Radius="10"
                Opacity="0.30" />
                </Button.Shadow>

            </Button>

        </VerticalStackLayout>



        <Border StrokeShape="RoundRectangle 20" BackgroundColor="#D9D9D9"
          Grid.Row="1"
          Grid.Column="0"
          Grid.ColumnSpan="2"
          WidthRequest="900"
          Margin="45,0,0,0">
            <CollectionView
        x:Name="list"
      
        ItemsSource="{Binding Products}"
        SelectionMode="Single"
        Style="{StaticResource listStyle}"
                BackgroundColor="#D9D9D9">

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border
                    x:Name="itemBorder"
                    Padding="10"
                    BackgroundColor="Transparent">

                            <Label
                        Text="{Binding Name}"
                        TextColor="DarkRed" />

                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup Name="CommonStates">
                                    <VisualState Name="Normal">
                                        <VisualState.Setters>
                                            <Setter
                                        TargetName="itemBorder"
                                        Property="BackgroundColor"
                                        Value="Transparent" />
                                        </VisualState.Setters>
                                    </VisualState>

                                    <VisualState Name="Selected">
                                        <VisualState.Setters>
                                            <Setter
                                        TargetName="itemBorder"
                                        Property="BackgroundColor"
                                        Value="#e6be8f" />
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>

                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Border>


       
        
    </Grid>
</ContentPage>
